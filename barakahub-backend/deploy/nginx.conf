server {
    listen 80;
    server_name api.barakahub.pages.dev;  # Will be updated with real domain

    # Redirect HTTP to HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.barakahub.pages.dev;  # Will be updated with real domain

    # SSL (Let's Encrypt auto-renew via certbot)
    ssl_certificate /etc/letsencrypt/live/api.barakahub.pages.dev/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.barakahub.pages.dev/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api:10m rate=30r/s;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        limit_req zone=api burst=50 nodelay;
    }

    # Large uploads for audio/images
    client_max_body_size 50M;
}
